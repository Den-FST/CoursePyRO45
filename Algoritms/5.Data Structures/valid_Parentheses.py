from intro import Stack

# write a function which will return True if a set of Parentheses are valid and False otherwise
# ()()() - valid
# (()()) - valid
# ) - invalid
#  (() - invalid
# ((())) - valid
# ()(()) -> valid
# (() - invalid
# )))((( -> invalid
# (((())())) -> valid

# [(,(,(,]

def valid_paranthesis(expresion):
    pass